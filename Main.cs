using Godot;
using System;

public class Main : Node {
	// Called when the node enters the scene tree for the first time.
	public override void _Ready() {
		// Prints "Hello, World!" to the output window.
		GD.Print("Hello, World!");
	}
}
